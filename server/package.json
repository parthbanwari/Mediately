{
  "name": "server",
  "version": "1.0.0",
  "main": "app.js",
  "type": "module",
 "scripts": {
    "start": "node app.js",
    "server": "nodemon server/app.js",
    "build": "npm install && cd .. && cd ./client && npm install && npm run build && cd .. && cd ./server && mkdir build && cp -a ../client/build/. ./build/"
  },
  
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "firebase-admin": "^13.4.0",
    "socket.io": "^4.8.1"
  }
}
